#!/usr/bin/env bash
#
# If you are reading this, you have wandered out of bounds
# and are reading the code that drives the game.
#
#                    Congratulations!
#
# Learning Linux is all about curiosity, so read this code and see
# if you can figure out what it does.
#
# When you're ready to continue playing the game, though, stick to
# the scrolls. If you're stuck, go to Gitlab and create an issue.
# We're happy to provide hints.
#

if ! [ -L portal ]; then
    cat << EOF
Runes, the language of the ancient mystics that once ruled
this land, are inscribed upon the western wall.

EOF
    printf "Do you want to read them? y/n  "
    read RESP

    if [ "$RESP" = "y" -o "$RESP" = "Y" ]; then
        cat << EOF
You recall the lessons of Caitlyn the Green, who taught you
these ancient letters.

The runes are instructions on how to summon a PORTAL — a
magickal shortcut to a distant room.

In the terminal, this is called a SYMBOLIC LINK (symlink).
A symlink is a special file that points to another location,
like a portal that teleports you somewhere else.

The command to create one is:

    ln -s TARGET LINKNAME

  TARGET   = where the portal LEADS (the destination)
  LINKNAME = what you call the portal here

To create a portal from this chamber to the hall:

    ln -fs ../../../chapel/courtyard/aviary/hall portal

Once created, walk through it like any other door:

    cd portal

To learn more about symlinks, visit the SCRAP room
(accessible from the entrance).

EOF
    else
        cat << EOF
You decide it is probably best to leave magickal
inscriptions as they are.

EOF
    fi
else
    cat << EOF
The invoked portal is still there, wide open as it has been
since its first appearance.

You can go through it just like any other door :

cd portal

EOF
fi



# ========================================================================
# If you are seeing this, you used 'cat' to read an executable file.
# That is great curiosity! But to USE this item, run it with:
#
#     ./spell
#
# Files marked with * in 'ls -F' output are executables (programs).
# The ./ prefix means "run the file right here in this directory."
# ========================================================================
