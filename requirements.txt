# Bashcrawl — Runtime Dependencies
# ==================================
# The game is pure bash and needs NO Python to play (cd entrance && cat scroll).
# Python is only needed to build the static web bundle and to run the lean MCP
# playtest harness:
#   pip install -r requirements.txt

# Static web export + content-contract validators
pyyaml>=6.0

# MCP server for the blank-slate playtest harness (bashcrawl_start/observe/...)
mcp>=1.0.0
