# Bashcrawl — Development & Testing Dependencies
# =================================================
# Install on top of runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt

# Test runner
pytest>=8.0
pytest-timeout>=2.3

# Schema validation (used in tests)
jsonschema>=4.20

# Python static checks
ruff>=0.13.0
