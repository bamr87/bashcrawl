{
  "mcpServers": {
    "bashcrawl": {
      "command": "python3",
      "args": ["-m", "ti.mcp_server"],
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src/terminal-illness"
      }
    }
  }
}
