# Bashcrawl Testing Framework Dependencies
# ==========================================
# For the full project, use the root requirements.txt + requirements-dev.txt instead.

# Test runner
pytest>=8.0
pytest-timeout>=2.3

# Schema validation
jsonschema>=4.20

# Static web export / content contracts
pyyaml>=6.0

# Playtest-harness smoke tests (playtest.mcp_server)
mcp>=2.1.1
